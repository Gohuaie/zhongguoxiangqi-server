# 中国象棋后端 GitHub + Render 部署指南

适用项目：

- GitHub：`https://github.com/Gohuaie/zhongguoxiangqi-server`
- 分支：`main`
- Render 服务：`zhong-guo-xiang-qi-yun-duan-fu-wu-qi`
- AppID：`wxf662c5f8d6bde5f5`
- WebSocket：`wss://zhong-guo-xiang-qi-yun-duan-fu-wu-qi.onrender.com`

本文件夹是“后端 GitHub 仓库根目录上传包”。后端上线后，还必须使用 v12 完整项目中的 `微信小游戏` 目录重新上传体验版，因为新版前端增加了 `wx.login` 和内容拦截提示；只更新后端不能完成整套整改。

## 一、先保存 AppSecret

1. 登录微信公众平台，进入当前小游戏。
2. 打开“开发管理/开发设置”，找到 AppSecret。
3. 如果无法查看旧值，就重置并立即复制到密码管理器。
4. 不要把 AppSecret 写入 GitHub、小游戏源码、截图或聊天记录。

## 二、更新 GitHub 仓库

1. 打开 `https://github.com/Gohuaie/zhongguoxiangqi-server`。
2. 切换到 `main` 分支。
3. 点击 `Add file` → `Upload files`。
4. 将本部署包中的所有文件拖入上传区。`云端联网.py`、`requirements.txt` 和 `部署说明.md` 选择覆盖同名文件。
5. 确认以下文件都位于仓库根目录，不要再套一层文件夹：

   - `云端联网.py`
   - `requirements.txt`
   - `违禁词.txt`
   - `违禁词来源说明.md`
   - `第三方词库-LICENSE.txt`
   - `render.yaml`
   - `.python-version`
   - `.gitignore`

6. Commit message 填写：`接入微信内容安全API并更新违禁词库`。
7. 选择 `Commit directly to the main branch`，点击提交。
8. 仓库中旧的 `__pycache__` 不参与运行，建议进入该目录逐个删除已提交的缓存文件；`.gitignore` 会阻止以后再次提交。

## 三、配置现有 Render 服务

不要再创建第二个 Web Service，也不要点“New Blueprint”。使用当前已经连接 GitHub 的服务：

1. 登录 Render Dashboard，打开 `zhong-guo-xiang-qi-yun-duan-fu-wu-qi`。
2. 打开 `Settings`，核对：

   - Repository：`Gohuaie/zhongguoxiangqi-server`
   - Branch：`main`
   - Root Directory：留空
   - Build Command：`pip install -r requirements.txt`
   - Start Command：`python 云端联网.py`
   - Auto-Deploy：`Yes` 或 `On Commit`
   - Health Check Path：`/health`

3. 打开左侧 `Environment`，逐项添加：

   | Key | Value |
   | --- | --- |
   | `PYTHON_VERSION` | `3.11.11` |
   | `WECHAT_APPID` | `wxf662c5f8d6bde5f5` |
   | `WECHAT_APPSECRET` | 在微信公众平台取得的真实 AppSecret |
   | `WECHAT_CONTENT_SECURITY_ENABLED` | `1` |
   | `WECHAT_SECURITY_FAIL_CLOSED` | `1` |
   | `DISCONNECT_GRACE_SECONDS` | `30` |
   | `SEND_TIMEOUT_SECONDS` | `3` |
   | `CHAT_SECURITY_TIMEOUT_SECONDS` | `7` |
   | `LOG_LEVEL` | `INFO` |

4. 检查 Key 前后没有空格，AppSecret 不要带引号。
5. 点击 `Save, rebuild, and deploy`。

## 四、观察自动部署

1. 打开 Render 的 `Events` 或 `Deploys` 页面。
2. 等待最新提交从 `Building` 变成 `Live`。
3. 日志中应出现安装 `websockets==11.0.3`，随后出现服务器监听日志。
4. 若没有自动部署，点击 `Manual Deploy` → `Deploy latest commit`。
5. 若提示找不到 `云端联网.py`，说明文件没有放在仓库根目录，或 Start Command 拼写不一致。

## 五、验证后端

在浏览器打开：

- `https://zhong-guo-xiang-qi-yun-duan-fu-wu-qi.onrender.com/health`
- `https://zhong-guo-xiang-qi-yun-duan-fu-wu-qi.onrender.com/security-status`

第一个页面必须显示 `OK`。第二个页面应类似：

```json
{
  "wechat_content_security_enabled": true,
  "wechat_credentials_configured": true,
  "fail_closed": true,
  "forbidden_words": 15660
}
```

也可在电脑运行：

```powershell
python 部署后检查.py
```

如果 `wechat_credentials_configured` 为 `false`，回到 Render Environment 检查 AppID 和 AppSecret，然后重新部署。

## 六、Render 出口 IP

你提供的当前出口网段为：

```text
74.220.48.0/24
74.220.56.0/24
```

Render 服务可能从这两个网段内的任意地址访问微信接口，不要只选网段中的某一个普通 IP。小游戏接口通常不要求配置公众号式 IP 白名单；先完成部署并进行真机检测。如果 Render 日志明确返回微信错误码 `40164`：

1. 在微信公众平台找到该小游戏的 IP 白名单设置。
2. 如果输入框接受 CIDR，逐行填写上述两个 `/24` 网段。
3. 如果微信页面只接受单个 IP，不要手工填写 512 个地址；改用 Render Dedicated Outbound IP，再把专属 IP 加入微信白名单。

## 七、微信服务器域名

你提供的截图已经确认 `socket合法域名` 为：

```text
wss://zhong-guo-xiang-qi-yun-duan-fu-wu-qi.onrender.com
```

当前小游戏没有用 `wx.request`、上传文件或下载用户文件访问自建后端，因此 `request合法域名`、`uploadFile合法域名` 和 `downloadFile合法域名` 可以暂时留空。`wx.login` 是微信原生接口，不需要把 `api.weixin.qq.com` 填入小游戏 request 合法域名。

## 八、真机验证内容安全

必须用微信开发者工具的“预览/真机调试”测试，不要只用浏览器 HTML 版：

1. 在微信开发者工具中导入 v12 完整项目的 `微信小游戏` 目录，确认 AppID 为 `wxf662c5f8d6bde5f5`，然后清除缓存并重新编译。
2. 设置普通昵称，应保存成功。
3. 设置明显违规昵称，应看到“昵称不可用”且不能保存。
4. 发送普通聊天，应正常出现在双方交流区。
5. 发送测试用违规内容，应提示“内容未发送”，其他玩家和观战者均不能看到，刷新后聊天历史中也不能出现。
6. 查看 Render Logs，确认没有 `40001`、`40125`、`40164`、`41001`、`87014` 之外的配置错误；`87014` 表示内容被安全接口识别并拦截，属于预期结果。

## 九、提交审核

1. 上传整改后小游戏版本。
2. 版本说明填写：`接入微信内容安全API，对昵称及聊天室内容执行先审后发；有害、待复核及检测异常内容均拦截。`
3. 审核备注说明当前没有用户图片上传、个人签名或个人空间描述功能；后端已经预留 `imgSecCheck`，未来增加图片 UGC 时执行先审后存。
4. 可将项目中的 `微信审核整改说明.md` 内容整理后粘贴到审核备注。

## 十、常见故障

- 聊天全部提示“内容安全服务暂时不可用”：检查 AppSecret、Render 日志及微信接口错误码。
- `40001` 或 `40125`：AppSecret 错误或与 AppID 不匹配，重新复制或重置。
- `40164`：出口 IP 不在允许范围，按第六节处理。
- `/health` 无法打开：检查构建是否成功、Start Command 和 `PORT` 监听。
- `/security-status` 返回 404：Render 尚未部署新版本，或 GitHub 文件未覆盖成功。
- 第一次连接慢：免费实例可能休眠，等待状态恢复后刷新房间列表；如需稳定审核和实时对局，建议使用不会休眠的实例计划。
