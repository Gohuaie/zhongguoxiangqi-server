# 中国象棋联机后端

## 启动

```bash
pip install -r requirements.txt
python 云端联网.py
```

环境变量：

- `PORT`：监听端口，默认 `10000`。
- `DISCONNECT_GRACE_SECONDS`：玩家断线后保留角色的秒数，默认 `30`，最小 `5`。
- `SEND_TIMEOUT_SECONDS`：向单个慢连接发送消息的超时秒数，默认 `3`。
- `CHAT_SECURITY_TIMEOUT_SECONDS`：聊天内容安全审核等待上限，默认 `7`；超时内容不会发布。
- `LOG_LEVEL`：日志级别，默认 `INFO`。

## Render 配置建议

- Build Command：`pip install -r requirements.txt`
- Start Command：`python 云端联网.py`
- Health Check Path：`/health`

Render 免费实例休眠后首次唤醒可能较慢，这是平台冷启动行为。前端已经增加 30-60 秒等待及刷新列表提示；若希望消除冷启动，需要改用不会休眠的实例。

## 本次优化

- 权威快照同时保存棋盘版本、进行/结束状态、胜方和结束原因；玩家离线期间发生走棋或认输，重连后会恢复到最新状态。
- 走棋使用消息 ID 和递增版本号确认，重复发送不会重复落子，版本不一致会返回权威棋盘。
- 每位接收者每步只接收一次走棋更新，不再重复广播完整棋盘。
- 聊天和昵称安全审核改为后台任务，不再阻塞同一连接的走棋、房间列表和心跳。
- 聊天仍然必须通过微信内容安全检测后才会广播；前端等待期间显示“审核中”。
- 单个慢连接发送超过 3 秒会被断开并进入重连流程，避免拖慢整个房间。
- 玩家和观战者都支持断线恢复，异常席位默认保留 30 秒。
- 大厅房间列表在建房、加入、选边、退出和断线时主动广播。
- 玩家意外断线后保留角色 30 秒，并支持恢复对局和棋盘同步。
- 显式退出立即释放角色；空房间自动清理。
- 清理已断开的客户端记录，避免长期运行内存泄漏。
- 阻止观战者及未选边玩家伪造走棋或对局操作。
- 校验选边、走棋坐标、JSON 类型和消息大小。
- 增加 WebSocket 心跳、关闭超时、日志及 `/health` 健康检查。

本次修改前的源码备份为 `云端联网.py.bak-20260907`。

## v3 会话与观战同步

- 选边时绑定客户端会话 ID，同一玩家的新连接可以安全接管分享前的旧连接。
- 后端保存当前棋盘、回合、最后一步、损失棋子及悔棋历史。
- 观战者加入和玩家重连时直接获得权威快照。
- 悔棋完成后向全房间重新广播快照，修正中途加入观战者的历史不足问题。
- 再来一局时广播 `new_round`，确保所有观战端统一重置。
- 后端保存并清洗玩家昵称和短消息，昵称最多 10 个字符，消息最多 30 个字符。

本轮修改前的源码备份为 `云端联网.py.bak-20260907-v2`。前端与后端必须同步发布。

## v4 固定交流区

- 每个房间保存最近 50 条交流记录，新加入者和断线重连者会自动收到历史消息。
- 观战连接可以设置昵称并发送消息，但仍不能走棋或执行悔棋、求和等对局操作。
- 消息继续执行服务端清洗：昵称最多 10 个字符，内容最多 30 个字符。

## v5 规则与 HTML 作弊器协议

- 普通玩家由服务端校验当前回合、棋子归属及是否覆盖己方棋子。
- HTML 玩家发送 `set_cheat_mode` 开启作弊器后，可无视回合和棋子归属移动任意棋子，也可以覆盖任意目标格。
- 作弊器只对主动开启它的连接生效；重连时 HTML 会重新同步本地开关状态。
- “送将”限制属于前端规则，v5 小游戏和 HTML 普通模式均已取消该限制。

## v6 强制权威同步

- 每次走棋后向其他客户端发送规范化移动消息，明确标记是否为作弊移动及棋子实际阵营。
- 随后向房间内所有连接（包括走棋方和观战者）广播 `force: true` 的权威棋盘快照。
- 两端以服务端棋盘、回合、最后一步和损失棋子为准，修复 HTML 作弊走棋与小程序画面不同步的问题。

## v7 交流身份

- 聊天消息增加权威 `role` 字段：红方、黑方以及按加入顺序编号的观战者。
- 服务器将消息回传给发送者，所有客户端只展示服务器确认后的同一条消息，避免重复或身份不一致。
- HTML 主动切换作弊模式时广播并保存系统提示，所有玩家和观战者均可看到。

## v11 违禁词配置

- 服务端拒绝含违禁词的昵称，并向客户端返回 `nickname_rejected`。
- 聊天内容中的违禁词替换为等长星号，过滤后的内容才会广播并写入最近 50 条历史消息。
- 编辑同目录下的 `违禁词.txt` 可维护词库：每行一个词，空行及以 `#` 开头的行会被忽略；修改后需重启服务。
- 也可通过环境变量 `BLOCKED_WORDS` 临时追加多个词，词之间使用英文逗号分隔。
- 前端与后端应同时发布；后端词库是最终权威检查。

## 微信官方内容安全 API（审核必需）

在云端后端设置以下环境变量后重启：

```text
WECHAT_APPID=你的小程序AppID
WECHAT_APPSECRET=你的小程序AppSecret
WECHAT_CONTENT_SECURITY_ENABLED=1
WECHAT_SECURITY_FAIL_CLOSED=1
```

- 小游戏通过 `wx.login` 提交一次性 code，后端调用 `jscode2session` 获取 openid；禁止把 AppSecret 放入前端。
- 昵称发布前调用 `msgSecCheck` 的资料场景，聊天室消息广播及入库前调用评论场景。
- 微信返回 `risky` 或 `review` 时均拦截；接口异常时默认拒绝发布。
- 后端已提供 `imgSecCheck` 检测函数，但当前产品没有任何用户图片上传入口。未来增加头像、图片聊天等功能时，必须先检测再保存或展示。
- `WECHAT_CONTENT_SECURITY_ENABLED=0` 只允许本地自动化测试使用，正式环境不可关闭。
- 在微信公众平台配置后端出口 IP 白名单；若当前云服务没有固定出口 IP，应先配置固定出口或迁移到具有固定出口 IP 的服务，否则获取 `access_token` 可能失败，fail-closed 策略会暂停昵称及聊天发布。
- 部署后访问 `/security-status`，确认 `wechat_content_security_enabled`、`wechat_credentials_configured` 和 `fail_closed` 均为 `true`，且 `forbidden_words` 大于 15000；该接口不会返回 AppSecret。
